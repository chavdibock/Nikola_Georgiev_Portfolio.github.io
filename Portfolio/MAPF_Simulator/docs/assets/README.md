Drop screenshots referenced by the top-level `README.md` in here:

- `dashboard.png` — the live browser dashboard (`http://127.0.0.1:8000`) while a session is running.
  A good shot: scenario = `surge`, mode = `Joint agent environment`, mid-run, with a few AV (orange)
  and human (blue) dots visible and the live metrics / active rewards panels populated.
- `replay.png` — optional, the same viewer while `make replay` is driving it with trained checkpoints.

Any image dropped here with these filenames will show up automatically in the README — no other
changes needed.
